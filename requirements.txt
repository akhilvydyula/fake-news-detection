# Prefer a single install line (editable app + dev tools):
#   pip install -e ".[dev]"
# See also: pyproject.toml, Makefile, tasks.py (Invoke).
#
# Core ML / data
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0
datasets>=3.0.0
huggingface_hub>=0.24.0

# Deep learning (TensorFlow / Keras)
tensorflow>=2.16.0

# NLP utilities
regex>=2024.0.0

# API + UI backend
django>=5.0
pydantic>=2.0.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
feedparser>=6.0.11
streamlit>=1.40.0

# Experiment tracking (optional at runtime; used when --mlflow is passed)
mlflow>=2.16.0

# Dev / CI
pytest>=8.0.0
pytest-django>=4.8.0
wheel>=0.46.2
